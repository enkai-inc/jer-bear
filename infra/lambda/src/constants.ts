// Shared constants for the Jer-Bear Lambda functions (api + notification-checker).
// CAREGIVER_CODE_LENGTH is mirrored in mobile/src/constants.ts — keep them in sync.

export const CAREGIVER_CODE_LENGTH = 6;
export const DEFAULT_TIMEZONE = 'America/New_York';
